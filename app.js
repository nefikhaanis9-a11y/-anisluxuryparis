### Ran Playwright code
```js
await page.goto('https://anis-luxury-paris.netlify.app/app.js');
```
### Page
- Page URL: https://anis-luxury-paris.netlify.app/app.js
### Snapshot
```yaml
- generic [ref=e2]: "// ============================================================ // CATALOGUE PRODUITS (embarqué — fonctionne en file:// et http://) // ============================================================ const CATALOG = { \"Louis Vuitton\": { \"🛍️ Sacs\": [ \"Speedy 20\",\"Speedy 25\",\"Speedy 30\",\"Speedy 35\",\"Speedy 40\", \"Speedy Bandoulière 20\",\"Speedy Bandoulière 25\",\"Speedy Bandoulière 30\", \"Nano Speedy\", \"Neverfull PM\",\"Neverfull MM\",\"Neverfull GM\", \"Alma BB\",\"Alma PM\",\"Alma MM\",\"Alma GM\", \"Pochette Métis\",\"Pochette Métis East West\", \"Félicie Pochette\",\"Multi Pochette Accessoires\", \"Neonoe BB\",\"Neonoe MM\", \"Onthego PM\",\"Onthego MM\",\"Onthego GM\", \"Twist PM\",\"Twist MM\", \"Capucines BB\",\"Capucines PM\",\"Capucines MM\",\"Capucines GM\", \"Montaigne BB\",\"Montaigne MM\",\"Montaigne GM\", \"Petite Malle\",\"Malle Minaudière\", \"Boite Chapeau Souple MM\",\"Boite Chapeau Souple PM\", \"Lockit BB\",\"Lockit PM\",\"Lockit MM\", \"Lockme BB\",\"Lockme Bucket\",\"Lockme Ever BB\",\"Lockme Ever MM\", \"Carryall MM\",\"Carryall PM\", \"Keepall 45\",\"Keepall 50\",\"Keepall 55\",\"Keepall 60\", \"Keepall Bandoulière 45\",\"Keepall Bandoulière 50\",\"Keepall Bandoulière 55\", \"Totally PM\",\"Totally MM\",\"Totally GM\", \"Palermo PM\",\"Palermo GM\",\"Deauville\",\"Voltaire\",\"Bagatelle\", \"Favorite PM\",\"Favorite MM\", \"City Steamer PM\",\"City Steamer MM\", \"Vavin BB\",\"Vavin PM\",\"Vavin Chain\", \"Dauphine MM\",\"Dauphine Mini\",\"Maida Hobo\", \"Fold Tote MM\",\"Bella Tote\",\"Diane\", \"Go-14 MM\",\"Go-14 PM\", \"Coussin BB\",\"Coussin PM\",\"Loop\", \"New Wave Chain MM\",\"Victoire\", \"Brea MM\",\"Brea PM\",\"Brea GM\",\"Elipse PM\",\"Passy\", \"Georges BB\",\"Georges MM\",\"Kimono MM\",\"Kimono PM\", \"Very One Handle PM\",\"Side Trunk\", \"Sac Plat BB\",\"Sac Plat 24H\" ], \"👜 Maroquinerie\": [ \"Portefeuille Sarah\",\"Portefeuille Sarah NM\", \"Portefeuille Zippy\",\"Portefeuille Zippy XL\", \"Portefeuille Emilie\",\"Portefeuille Victorine\", \"Portefeuille Clemence\",\"Portefeuille Iris\",\"Portefeuille Iris Compact\", \"Portefeuille Juliette\",\"Portefeuille Curieuse\",\"Portefeuille Compact Zippy\", \"Porte-Monnaie Rosalie\",\"Porte-Monnaie Round\",\"Porte-Monnaie Zippy\", \"Porte-Cartes Recto Verso\",\"Porte-Cartes Simple\",\"Porte-Cartes Multiple\",\"Porte-Cartes Zippy\", \"Card Holder Enveloppe\", \"Pochette Accessoires\",\"Pochette Wristlet\",\"Mini Pochette Accessoires\", \"Nano Pochette\",\"Pochette Double Zip\",\"Pochette Kirigami\", \"Agenda PM\",\"Agenda MM\",\"Agenda GM\", \"Couverture Passeport\",\"Étui Cartes de Visite\", \"Ceinture Initiales 30mm\",\"Ceinture Initiales 35mm\",\"Ceinture Initiales 40mm\", \"Ceinture LV Initiales 35mm\",\"Ceinture LV Initiales 40mm\",\"Ceinture Reversible\", \"Étui à lunettes\", \"Toiletry Pouch 15\",\"Toiletry Pouch 19\",\"Toiletry Pouch 26\", \"Zippy Coin Purse\",\"Insolite Coin Purse\" ], \"🔑 Accessoires\": [ \"Cadenas 100 Brass\",\"Cadenas 105 Brass\",\"Cadenas 107 Brass\", \"Cadenas Gold\",\"Cadenas Silver\",\"Clochette & Clef\", \"Porte-Clés Anneau\",\"Porte-Clés Chien\",\"Porte-Clés LV Circle\", \"Porte-Clés Dragonne\",\"Porte-Clés Clé de Rêves\", \"Bag Charm Speedy\",\"Bag Charm Monogram\",\"Bijou de Sac Bandoulière\", \"Foulard Monogram\",\"Foulard Confidential\",\"Châle Monogram\", \"Chapeau Bob\",\"Bracelet LV Instinct\",\"Collier LV Instinct\",\"Bague Berg\", \"Lunettes de soleil LV\",\"Bandeau\" ], \"👠 Chaussures\": [ \"Sneakers Archlight\",\"Sneakers Run Away\",\"Sneakers LV Squad\", \"Sneakers Frontrow\",\"Sneakers Beverly Hills\", \"Escarpins Oh Really!\",\"Escarpins Sparkle\", \"Boots Laureate Platform\",\"Mocassins Hockenheim\",\"Ballerines\" ] }, \"Chanel\": { \"🛍️ Sacs\": [ \"Classic Flap Mini\",\"Classic Flap Small\",\"Classic Flap Medium\",\"Classic Flap Maxi\",\"Classic Flap Jumbo\", \"2.55 Reissue 224\",\"2.55 Reissue 225\",\"2.55 Reissue 226\",\"2.55 Reissue 227\", \"Boy Nano\",\"Boy Small\",\"Boy Medium\",\"Boy Old Medium\",\"Boy Large\", \"WOC (Wallet on Chain)\", \"Gabrielle Hobo Small\",\"Gabrielle Hobo Medium\",\"Gabrielle Hobo Large\", \"Deauville Small\",\"Deauville Medium\",\"Deauville Grand\", \"Coco Handle Small\",\"Coco Handle Medium\",\"Coco Handle Large\", \"22 Mini\",\"22 Small\",\"22 Medium\",\"22 Large\", \"19 Small\",\"19 Medium\",\"19 Large\", \"Vanity Case Small\",\"Vanity Case Medium\", \"Trendy CC Small\",\"Trendy CC Medium\", \"Clutch Minaudière\",\"Shopping Bag Grand\",\"Gabrielle Backpack\", \"Flap Bag With Top Handle Small\",\"Pearl Crush\" ], \"👜 Maroquinerie\": [ \"Portefeuille Classic\",\"Portefeuille Zippy\",\"Card Holder\",\"Porte-Monnaie\",\"Pochette\" ], \"🔑 Accessoires\": [ \"Bijoux Coco Crush\",\"Lunettes de soleil\",\"Foulard\",\"Broche\" ] }, \"Hermès\": { \"🛍️ Sacs\": [ \"Birkin 25\",\"Birkin 30\",\"Birkin 35\",\"Birkin 40\", \"Kelly 20 Mini\",\"Kelly 25\",\"Kelly 28\",\"Kelly 32\",\"Kelly 35\",\"Kelly Picnic\",\"Kelly 2424\", \"Constance 18 Mini\",\"Constance 24\",\"Constance 28\", \"Evelyne TPM\",\"Evelyne PM\",\"Evelyne MM\",\"Evelyne GM\", \"Picotin Lock 14 Mini\",\"Picotin Lock 18 PM\",\"Picotin Lock 22 MM\", \"Lindy 26\",\"Lindy 30\", \"Garden Party 30\",\"Garden Party 36\", \"Bolide 25\",\"Bolide 31\", \"Trim I\",\"Trim II\",\"Roulis 18\",\"Roulis 23\", \"Toolbox 20\",\"Toolbox 26\",\"Halzan 31\", \"Jypsiere 28\",\"Jypsiere 31\", \"Herbag Zip 31\",\"Herbag Zip 39\",\"Victoria II\" ], \"👜 Maroquinerie\": [ \"Portefeuille Béarn\",\"Portefeuille Dogon\",\"Porte-Cartes\",\"Ceinture Reversible\",\"Agenda\" ], \"🔑 Accessoires\": [ \"Carré 90\",\"Carré 70\",\"Twilly\", \"Bracelet Clic Clac\",\"Bracelet Kelly\",\"Bague Collier de Chien\",\"Cadenas Hermès\" ] }, \"Dior\": { \"🛍️ Sacs\": [ \"Lady Dior Micro\",\"Lady Dior Mini\",\"Lady Dior Small\",\"Lady Dior Medium\",\"Lady Dior Large\", \"Book Tote Small\",\"Book Tote Medium\",\"Book Tote Large\", \"Saddle Bag Mini\",\"Saddle Bag\", \"30 Montaigne Mini\",\"30 Montaigne\", \"Diorama Mini\",\"Diorama Small\", \"Dior Caro Mini\",\"Dior Caro Small\",\"Dior Caro Medium\", \"Bobby East-West\",\"Bobby Small\",\"Bobby Large\", \"Toujours Compact\",\"Toujours Large\", \"Dior Vibe Hobo\",\"Dior Bucket Bag\", \"Dior Chez Moi Small\",\"Dior Chez Moi Large\" ], \"👜 Maroquinerie\": [ \"Portefeuille Lady Dior\",\"Portefeuille Saddle\",\"Card Holder\",\"Ceinture\" ], \"🔑 Accessoires\": [ \"Foulard Oblique\",\"Lunettes de soleil\",\"Bijoux CD\" ] }, \"Gucci\": { \"🛍️ Sacs\": [ \"GG Marmont Mini\",\"GG Marmont Small\",\"GG Marmont Medium\",\"GG Marmont Large\", \"Dionysus Mini\",\"Dionysus Small\",\"Dionysus Medium\", \"Ophidia Mini\",\"Ophidia Small\",\"Ophidia Medium\",\"Ophidia GG\", \"Jackie 1961 Mini\",\"Jackie 1961 Small\",\"Jackie 1961 Medium\", \"Bamboo 1947 Mini\",\"Bamboo 1947 Small\", \"Horsebit 1955 Mini\",\"Horsebit 1955 Small\",\"Horsebit 1955 Medium\", \"Attache Small\",\"Attache Medium\", \"Blondie Mini\",\"Blondie Small\",\"Luce Small\",\"GG Matelassé Small\", \"Tote Bag GG Supreme\",\"Sylvie Mini\",\"Sylvie Small\",\"Padlock Small\" ], \"👜 Maroquinerie\": [ \"Portefeuille GG Marmont\",\"Portefeuille Ophidia\",\"Card Case GG\",\"Ceinture GG\",\"Ceinture Marmont\" ] }, \"Saint Laurent\": { \"🛍️ Sacs\": [ \"Sac de Jour Nano\",\"Sac de Jour Small\",\"Sac de Jour Medium\",\"Sac de Jour Large\", \"Lou Mini\",\"Lou Camera\", \"Kate Small\",\"Kate Medium\", \"Sunset Small\",\"Sunset Medium\", \"Loulou Toy\",\"Loulou Small\",\"Loulou Medium\", \"Puffer Mini\",\"Puffer Small\",\"Puffer Medium\", \"Icare Maxi\",\"Gaby\",\"5 à 7\",\"Jamie\", \"Niki Baby\",\"Niki Small\",\"Niki Medium\", \"Solferino Small\",\"Solferino Medium\", \"Envelope Small\",\"Monogramme Clutch\",\"Vanity Bag\" ], \"👜 Maroquinerie\": [ \"Portefeuille Monogramme\",\"Portefeuille Cassandre\",\"Card Case\",\"Ceinture YSL\" ] }, \"Balenciaga\": { \"🛍️ Sacs\": [ \"City Mini\",\"City Small\",\"City Medium\",\"City Large\", \"Le Cagole XS\",\"Le Cagole Small\",\"Le Cagole Medium\", \"Hourglass Mini\",\"Hourglass Small\",\"Hourglass Medium\",\"Hourglass XL\", \"Neo Classic Mini\",\"Neo Classic Small\",\"Neo Classic Medium\", \"Papier A4\",\"Papier A6\", \"Triangle Small\",\"Triangle Medium\", \"Everyday Tote XS\",\"Everyday Tote S\",\"Everyday Tote M\", \"Rodeo Small\",\"Rodeo Medium\",\"Crush Small\",\"Crush Medium\",\"Xs BB\",\"Bucket\" ] }, \"Prada\": { \"🛍️ Sacs\": [ \"Galleria Small\",\"Galleria Medium\",\"Galleria Large\", \"Re-Edition 1995 Mini\",\"Re-Edition 2000 Nylon\", \"Re-Nylon Small\",\"Re-Nylon Medium\", \"Cleo Mini\",\"Cleo Small\",\"Triangle Small\",\"Cahier\",\"Odette\", \"Double Bag Small\",\"Symbole Small\",\"Symbole Medium\", \"Arqué Small\",\"Arqué Medium\",\"Tessuto Nylon Tote\" ], \"👜 Maroquinerie\": [ \"Portefeuille Saffiano\",\"Card Holder Saffiano\",\"Ceinture\" ] }, \"Fendi\": { \"🛍️ Sacs\": [ \"Baguette Mini\",\"Baguette Small\",\"Baguette Medium\", \"Peekaboo Mini\",\"Peekaboo XS\",\"Peekaboo Small\",\"Peekaboo Regular\", \"Mon Trésor Mini\",\"Mon Trésor Small\", \"First Small\",\"First Medium\",\"Way Small\", \"Sunshine Small\",\"Sunshine Medium\", \"O'Lock Mini\",\"O'Lock Medium\",\"Fendigraphy Small\",\"C'mon Small\",\"Iconic Backpack\" ] }, \"Céline\": { \"🛍️ Sacs\": [ \"Classic Box Small\",\"Classic Box Medium\", \"Luggage Nano\",\"Luggage Mini\",\"Luggage Micro\", \"Belt Nano\",\"Belt Mini\",\"Belt Bag\", \"Triomphe Chain Mini\",\"Triomphe Chain Medium\", \"Ava Mini\",\"Ava Medium\",\"Cabas\",\"Phantom\",\"Tilly\",\"Big Bag Bucket\" ] }, \"Bottega Veneta\": { \"🛍️ Sacs\": [ \"Jodie Mini\",\"Jodie Piccolo\",\"Jodie Small\",\"Jodie Medium\",\"Teen Jodie\", \"Pouch Mini\",\"Pouch Clutch\", \"Cassette Mini\",\"Cassette Small\", \"Arco 33\",\"Arco 48\",\"Sardine\",\"Loop\",\"Wallace Small\",\"Cobble\" ] }, \"Burberry\": { \"🛍️ Sacs\": [ \"TB Bag Small\",\"TB Bag Medium\",\"Lola Small\",\"Lola Medium\", \"Knight Small\",\"Check Tote Small\",\"Check Tote Medium\", \"Olympia Mini\",\"Olympia Small\",\"Note Crossbody\",\"Vintage Check Tote\" ] }, \"Valentino\": { \"🛍️ Sacs\": [ \"Rockstud Mini\",\"Rockstud Small\",\"Rockstud Medium\", \"Rockstud Spike Small\",\"One Stud Mini\",\"One Stud Bag\", \"Garavani VSling Small\",\"Garavani Loco Small\", \"Garavani Roman Stud Small\",\"Garavani Atelier Bag\" ] }, \"Givenchy\": { \"🛍️ Sacs\": [ \"Antigona Micro\",\"Antigona Mini\",\"Antigona Small\",\"Antigona Medium\", \"Antigona Soft Mini\",\"Antigona Soft Medium\",\"Antigona Lock Mini\", \"4G Small\",\"4G Medium\",\"Cut Out Small\",\"Kenny Small\",\"Voyou\",\"Tote 4G\" ] }, \"MCM\": { \"🛍️ Sacs\": [ \"Stark Backpack Mini\",\"Stark Backpack Medium\", \"Millie Small\",\"Millie Medium\",\"Patricia Small\",\"Patricia Medium\", \"Visetos Tote Small\",\"Aren Hobo\" ] }, \"Coach\": { \"🛍️ Sacs\": [ \"Tabby 26\",\"Tabby 20\",\"Pillow Tabby 18\", \"Brooklyn Soft Tote\",\"Dreamer 21\",\"Dreamer 36\", \"Willow Bucket\",\"Field Tote 22\",\"Swagger Mini\" ] }, \"Autre\": { \"✏️ Personnalisé\": [\"Autre / Saisie manuelle\"] } }; function loadCatalog() { /* catalogue déjà chargé en mémoire */ } // Fonction générique : remplit un select marque avec le catalogue function fillMarqueSelect(selId) { const sel = document.getElementById(selId); if (!sel) return; sel.innerHTML = '<option value=\"\">— Sélectionner une marque —</option>'; Object.keys(CATALOG).forEach(marque => { const opt = document.createElement('option'); opt.value = marque; opt.textContent = marque; sel.appendChild(opt); }); } // Fonction générique : remplit un select modèle selon la marque choisie function fillModeleSelect(marque, selModeleId) { const sel = document.getElementById(selModeleId); if (!sel) return; sel.innerHTML = '<option value=\"\">— Sélectionner un modèle —</option>'; if (!marque || !CATALOG[marque]) return; Object.entries(CATALOG[marque]).forEach(([cat, modeles]) => { const group = document.createElement('optgroup'); group.label = cat; modeles.forEach(m => { const opt = document.createElement('option'); opt.value = m; opt.textContent = m; group.appendChild(opt); }); sel.appendChild(group); }); const grpCustom = document.createElement('optgroup'); grpCustom.label = '✏️ Personnalisé'; const optCustom = document.createElement('option'); optCustom.value = '__custom__'; optCustom.textContent = 'Autre / Saisie manuelle'; grpCustom.appendChild(optCustom); sel.appendChild(grpCustom); } // Fonction générique : retourne le nom de l'article selon préfixe (v_ ou c_) function getArticleNameFor(prefix) { const marque = document.getElementById(prefix + '_marque')?.value || ''; const modele = document.getElementById(prefix + '_modele')?.value || ''; const custom = document.getElementById(prefix + '_article')?.value.trim() || ''; if (modele === '__custom__') return marque ? `${marque} — ${custom || 'Personnalisé'}` : custom; if (marque && modele) return `${marque} — ${modele}`; return custom || marque || modele; } // Fonction générique : gère affichage du champ personnalisé function handleModeleChange(modeleId, articleGroupId, articleInputId) { const modele = document.getElementById(modeleId).value; const group = document.getElementById(articleGroupId); const input = document.getElementById(articleInputId); if (modele === '__custom__') { group.style.display = 'flex'; input.value = ''; input.focus(); } else { group.style.display = 'none'; if (input) input.value = modele; } } // ── VENTES function initMarqueSelect() { fillMarqueSelect('v_marque'); } function onMarqueChange() { fillModeleSelect(document.getElementById('v_marque').value, 'v_modele'); document.getElementById('v_article').value=''; document.getElementById('v_article_group').style.display='none'; } function onModeleChange() { handleModeleChange('v_modele','v_article_group','v_article'); } function getArticleName() { return getArticleNameFor('v'); } // ── EN COURS DE VENTE function initECMarqueSelect() { fillMarqueSelect('ec_marque'); } function onECMarqueChange() { fillModeleSelect(document.getElementById('ec_marque').value, 'ec_modele'); document.getElementById('ec_article').value=''; document.getElementById('ec_article_group').style.display='none'; } function onECModeleChange() { handleModeleChange('ec_modele','ec_article_group','ec_article'); } function getECArticleName() { return getArticleNameFor('ec'); } // ── COMMANDES function initCMarqueSelect() { fillMarqueSelect('c_marque'); } function onCMarqueChange() { fillModeleSelect(document.getElementById('c_marque').value, 'c_modele'); document.getElementById('c_article').value=''; document.getElementById('c_article_group').style.display='none'; } function onCModeleChange() { handleModeleChange('c_modele','c_article_group','c_article'); } function getCArticleName() { return getArticleNameFor('c'); } // ============================================================ // BASE DE DONNÉES (localStorage) // ============================================================ // ============================================================ // SUPABASE — À REMPLIR avec vos clés du projet // ============================================================ const SUPABASE_URL = 'https://voixvhkdjceglzxjprok.supabase.co'; const SUPABASE_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InZvaXh2aGtkamNlZ2x6eGpwcm9rIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzQ1NTM4OTEsImV4cCI6MjA5MDEyOTg5MX0.NiIKQAyBHy4R9S_cZniGyMN4aOts0_mfD1SYMFOPTds'; const { createClient } = supabase; const sb = createClient(SUPABASE_URL, SUPABASE_KEY); let _uid = null; // ID utilisateur courant (défini après login) let db = { ventes: [], ventesEnCours: [], depenses: [], commandes: [], objectifs: { ca_annuel: 0, ca_mensuel: 0, benef_mensuel: 0, nb_ventes: 0 }, parametres: { urssaf: 12.3, seuil: 188700 } }; // ── Mappers base → camelCase function mapVente(r) { return { id: r.id, date: r.date, article: r.article, prixAchat: +r.prix_achat||0, prixVente: +r.prix_vente||0, portPaye: +r.port_paye||0, portRecu: +r.port_recu||0, plateforme: r.plateforme, notes: r.notes||'' }; } function mapVenteEnCours(r) { return { id: r.id, date: r.date, article: r.article, prixAchat: +r.prix_achat||0, prixDemande: +r.prix_demande||0, plateforme: r.plateforme, notes: r.notes||'' }; } function mapDepense(r) { return { id: r.id, date: r.date, categorie: r.categorie, desc: r.description, montant: +r.montant||0 }; } function mapCommande(r) { return { id: r.id, date: r.date, article: r.article, fournisseur: r.fournisseur||'', prixAchat: +r.prix_achat||0, statut: r.statut, notes: r.notes||'', dateReception: r.date_reception||null }; } async function fetchAllData() { if (!_uid) return; const [v, vec, dep, cmd, par] = await Promise.all([ sb.from('ventes').select('*').eq('user_id', _uid).order('date', { ascending: false }), sb.from('ventes_en_cours').select('*').eq('user_id', _uid).order('date', { ascending: false }), sb.from('depenses').select('*').eq('user_id', _uid).order('date', { ascending: false }), sb.from('commandes').select('*').eq('user_id', _uid).order('date', { ascending: false }), sb.from('parametres').select('*').eq('user_id', _uid).maybeSingle() ]); db.ventes = (v.data || []).map(mapVente); db.ventesEnCours = (vec.data || []).map(mapVenteEnCours); db.depenses = (dep.data || []).map(mapDepense); db.commandes = (cmd.data || []).map(mapCommande); if (par.data) { db.objectifs = { ca_annuel: +par.data.ca_annuel||0, ca_mensuel: +par.data.ca_mensuel||0, benef_mensuel: +par.data.benef_mensuel||0, nb_ventes: +par.data.nb_ventes||0 }; db.parametres = { urssaf: +par.data.urssaf||12.3, seuil: +par.data.seuil||188700 }; } } function dbErr(error, context) { console.error(context, error); alert('❌ Erreur : ' + (error?.message || error)); } // ============================================================ // UTILITAIRES // ============================================================ function fmt(n) { return new Intl.NumberFormat('fr-FR', { style: 'currency', currency: 'EUR' }).format(n || 0); } function today() { return new Date().toISOString().split('T')[0]; } function currentMonth() { return new Date().toISOString().slice(0, 7); } function currentYear() { return new Date().getFullYear(); } function getMonth(dateStr) { return dateStr ? dateStr.slice(0, 7) : ''; } function monthLabel(ym) { const [y, m] = ym.split('-'); const d = new Date(parseInt(y), parseInt(m) - 1, 1); return d.toLocaleDateString('fr-FR', { month: 'long', year: 'numeric' }); } function benefBrut(v) { return (v.prixVente || 0) - (v.prixAchat || 0) - (v.portPaye || 0) + (v.portRecu || 0); } function calcUrssaf(ca) { return (ca || 0) * (db.parametres.urssaf / 100); } function benefNet(v) { return benefBrut(v) - calcUrssaf(v.prixVente || 0); } // ============================================================ // NAVIGATION // ============================================================ function showTab(tab, btn) { document.querySelectorAll('.section').forEach(s => s.classList.remove('active')); document.querySelectorAll('nav button').forEach(b => b.classList.remove('active')); document.getElementById('tab-' + tab).classList.add('active'); if (btn) btn.classList.add('active'); renderTab(tab); } function renderTab(tab) { if (tab === 'dashboard') renderDashboard(); if (tab === 'ventes') { initECMarqueSelect(); initMarqueSelect(); renderVentesEnCours(); renderVentes(); } if (tab === 'depenses') renderDepenses(); if (tab === 'commandes') renderCommandes(); if (tab === 'stock') renderStock(); if (tab === 'urssaf') renderUrssaf(); if (tab === 'objectifs') renderObjectifs(); if (tab === 'parametres') renderParametres(); } function toggleCollapse(id) { const el = document.getElementById(id); const toggle = document.getElementById(id + 'Toggle'); if (el.style.display === 'none') { el.style.display = 'block'; toggle.textContent = '▲'; } else { el.style.display = 'none'; toggle.textContent = '▼'; } } // ============================================================ // DASHBOARD // ============================================================ function renderDashboard() { const now = currentMonth(); const year = currentYear(); document.getElementById('chartYear').textContent = year; const vAnnee = db.ventes.filter(v => v.date && v.date.startsWith(year)); const caAnnee = vAnnee.reduce((s, v) => s + (v.prixVente || 0), 0); const brutAnnee = vAnnee.reduce((s, v) => s + benefBrut(v), 0); const urssafAnnee = calcUrssaf(caAnnee); const depAnnee = db.depenses.filter(d => d.date && d.date.startsWith(year)) .reduce((s, d) => s + (d.montant || 0), 0); const netAnnee = brutAnnee - urssafAnnee - depAnnee; const vMois = db.ventes.filter(v => getMonth(v.date) === now); const caMois = vMois.reduce((s, v) => s + (v.prixVente || 0), 0); const brutMois = vMois.reduce((s, v) => s + benefBrut(v), 0); const urssafMois = calcUrssaf(caMois); const depMois = db.depenses.filter(d => getMonth(d.date) === now) .reduce((s, d) => s + (d.montant || 0), 0); const netMois = brutMois - urssafMois - depMois; const seuilPct = Math.min((caAnnee / db.parametres.seuil) * 100, 100).toFixed(1); // Commandes en cours const cmdEnCours = (db.commandes || []).filter(c => c.statut !== 'Arrivé'); const cmdCapital = cmdEnCours.reduce((s, c) => s + (c.prixAchat || 0), 0); document.getElementById('dashboardKPIs').innerHTML = ` <div class=\"dash-cat\"> <div class=\"dash-cat-header\"><span class=\"dash-cat-label\">📈 Produit</span></div> <div class=\"cards-grid\"> <div class=\"card gold-border\"> <div class=\"card-label\">CA — Mois en cours</div> <div class=\"card-value\">${fmt(caMois)}</div> <div class=\"card-sub\">${vMois.length} vente(s) ce mois</div> </div> <div class=\"card\"> <div class=\"card-label\">CA — Année ${year}</div> <div class=\"card-value\">${fmt(caAnnee)}</div> <div class=\"card-sub\">${seuilPct}% du seuil (${fmt(db.parametres.seuil)})</div> <div class=\"progress-bar-wrapper\"><div class=\"progress-bar\" style=\"width:${seuilPct}%\"></div></div> </div> <div class=\"card\"> <div class=\"card-label\">Nb ventes — Année</div> <div class=\"card-value blue\">${vAnnee.length}</div> <div class=\"card-sub\">${vMois.length} ce mois</div> </div> </div> </div> <div class=\"dash-cat\"> <div class=\"dash-cat-header\"><span class=\"dash-cat-label\">💸 Charges</span></div> <div class=\"cards-grid\"> <div class=\"card\"> <div class=\"card-label\">Dépenses — Mois</div> <div class=\"card-value red\">${fmt(depMois)}</div> <div class=\"card-sub\">Frais du mois en cours</div> </div> <div class=\"card\"> <div class=\"card-label\">URSSAF à provisionner</div> <div class=\"card-value blue\">${fmt(urssafMois)}</div> <div class=\"card-sub\">Taux : ${db.parametres.urssaf}% · mois en cours</div> </div> <div class=\"card\"> <div class=\"card-label\">Charges totales — Année</div> <div class=\"card-value red\">${fmt(depAnnee + urssafAnnee)}</div> <div class=\"card-sub\">Dép. ${fmt(depAnnee)} + URSSAF ${fmt(urssafAnnee)}</div> </div> ${cmdEnCours.length > 0 ? `<div class=\"card\"> <div class=\"card-label\">Commandes en transit</div> <div class=\"card-value\" style=\"color:var(--amber)\">${cmdEnCours.length}</div> <div class=\"card-sub\">Capital engagé : ${fmt(cmdCapital)}</div> </div>` : ''} </div> </div> <div class=\"dash-cat\"> <div class=\"dash-cat-header\"><span class=\"dash-cat-label\">🏆 Résultat final</span></div> <div class=\"cards-grid\"> <div class=\"card gold-border\"> <div class=\"card-label\">Bénéfice net — Mois</div> <div class=\"card-value ${netMois >= 0 ? 'green' : 'red'}\">${fmt(netMois)}</div> <div class=\"card-sub\">Brut : ${fmt(brutMois)}</div> </div> <div class=\"card gold-border\"> <div class=\"card-label\">Bénéfice net — Année ${year}</div> <div class=\"card-value ${netAnnee >= 0 ? 'green' : 'red'}\">${fmt(netAnnee)}</div> <div class=\"card-sub\">URSSAF année : ${fmt(urssafAnnee)}</div> </div> </div> </div> `; renderChart(year); // ── Trimestres URSSAF document.getElementById('dashUrssafYear').textContent = year; const currentM = new Date().getMonth() + 1; const currentT = Math.ceil(currentM / 3); const trimestres = [ { name: 'T1', months: ['01','02','03'], deadline: `30 avr. ${year}`, label: 'Jan · Fév · Mar' }, { name: 'T2', months: ['04','05','06'], deadline: `31 juil. ${year}`, label: 'Avr · Mai · Jun' }, { name: 'T3', months: ['07','08','09'], deadline: `31 oct. ${year}`, label: 'Jul · Aoû · Sep' }, { name: 'T4', months: ['10','11','12'], deadline: `31 janv. ${year+1}`, label: 'Oct · Nov · Déc' }, ]; document.getElementById('dashboardUrssafGrid').innerHTML = trimestres.map((t, i) => { const vT = db.ventes.filter(v => v.date && v.date.startsWith(year) && t.months.includes(v.date.slice(5,7))); const caT = vT.reduce((s, v) => s + (v.prixVente || 0), 0); const cotisT = calcUrssaf(caT); const isCurrent = (i + 1) === currentT; return `<div class=\"urssaf-trimestre ${isCurrent ? 'current' : ''}\"> <div style=\"display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:12px;\"> <div> <div style=\"font-size:15px;font-weight:700;color:var(--text)\">${t.name} ${isCurrent ? '<span class=\"badge badge-gold\" style=\"font-size:10px;margin-left:6px;vertical-align:middle\">EN COURS</span>' : ''} </div> <div style=\"font-size:11px;color:var(--text-muted);margin-top:2px\">${t.label}</div> </div> <div style=\"font-size:11px;color:var(--text-muted);text-align:right\">⏰ Échéance<br><strong style=\"color:var(--text);font-size:12px\">${t.deadline}</strong></div> </div> <div style=\"display:flex;justify-content:space-between;align-items:flex-end;\"> <div> <div style=\"font-size:10px;color:var(--text-muted);text-transform:uppercase;letter-spacing:0.5px\">CA</div> <div style=\"font-size:20px;font-weight:700;color:var(--gold)\">${fmt(caT)}</div> </div> <div style=\"text-align:right\"> <div style=\"font-size:10px;color:var(--text-muted);text-transform:uppercase;letter-spacing:0.5px\">Cotisations</div> <div style=\"font-size:20px;font-weight:700;color:var(--blue)\">${fmt(cotisT)}</div> </div> </div> <div style=\"font-size:11px;color:var(--text-muted);margin-top:8px;border-top:1px solid var(--border);padding-top:6px\">${vT.length} vente(s) ce trimestre</div> </div>`; }).join(''); // Dernières 5 ventes const dernieres = [...db.ventes].sort((a, b) => b.date.localeCompare(a.date)).slice(0, 5); if (dernieres.length === 0) { document.getElementById('recentSalesTable').innerHTML = `<div class=\"empty-state\"><div class=\"icon\">🛍️</div><p>Aucune vente enregistrée</p><p style=\"font-size:12px;margin-top:6px\">Allez dans l'onglet Ventes pour commencer</p></div>`; } else { document.getElementById('recentSalesTable').innerHTML = buildVentesTable(dernieres, false); } } // ── Tooltip graphique const _tt = () => document.getElementById('chartTooltip'); function showChartTooltip(e, label, ca, net) { const tt = _tt(); tt.innerHTML = ` <div class=\"tt-title\">${label}</div> <div class=\"tt-row\"><span style=\"color:var(--text-muted)\">CA</span><span style=\"color:var(--gold)\">${fmt(ca)}</span></div> <div class=\"tt-row\"><span style=\"color:var(--text-muted)\">Bénéfice net</span><span style=\"color:${net >= 0 ? 'var(--green)' : 'var(--red)'}\">${fmt(net)}</span></div> <div class=\"tt-row\"><span style=\"color:var(--text-muted)\">URSSAF est.</span><span style=\"color:var(--blue)\">${fmt(calcUrssaf(ca))}</span></div>`; tt.style.display = 'block'; moveChartTooltip(e); } function moveChartTooltip(e) { const tt = _tt(); const x = e.clientX + 14; const y = e.clientY - 10; const offX = (x + tt.offsetWidth > window.innerWidth) ? -tt.offsetWidth - 20 : 0; const offY = (y + tt.offsetHeight > window.innerHeight) ? -tt.offsetHeight + 20 : 0; tt.style.left = (x + offX) + 'px'; tt.style.top = (y + offY) + 'px'; } function hideChartTooltip() { _tt().style.display = 'none'; } let chartMode = 'mois'; // 'mois' | 'trim' function setChartMode(mode) { chartMode = mode; document.getElementById('btnChartMois').className = 'btn ' + (mode === 'mois' ? 'btn-gold' : 'btn-outline'); document.getElementById('btnChartTrim').className = 'btn ' + (mode === 'trim' ? 'btn-gold' : 'btn-outline'); document.getElementById('chartModeLabel').textContent = mode === 'mois' ? 'mensuelle' : 'trimestrielle'; renderChart(currentYear()); } function renderChart(year) { if (chartMode === 'trim') { renderChartTrim(year); return; } const months = Array.from({length: 12}, (_, i) => year + '-' + String(i + 1).padStart(2, '0')); const labels = ['Jan','Fév','Mar','Avr','Mai','Jun','Jul','Aoû','Sep','Oct','Nov','Déc']; const now = currentMonth(); const data = months.map(m => { const v = db.ventes.filter(x => getMonth(x.date) === m); const d = db.depenses.filter(x => getMonth(x.date) === m); const ca = v.reduce((s, x) => s + (x.prixVente || 0), 0); const brut = v.reduce((s, x) => s + benefBrut(x), 0); const dep = d.reduce((s, x) => s + (x.montant || 0), 0); const net = brut - calcUrssaf(ca) - dep; return { ca, net: Math.max(0, net) }; }); const maxVal = Math.max(...data.map(d => Math.max(d.ca, d.net)), 1); const container = document.getElementById('chartBars'); container.innerHTML = months.map((m, i) => { const d = data[i]; const caH = Math.round((d.ca / maxVal) * 100); const netH = Math.round((d.net / maxVal) * 100); const isCurrent = m === now; return ` <div class=\"chart-bar-group\" data-label=\"${labels[i]}\" data-ca=\"${d.ca}\" data-net=\"${d.net}\"> <div class=\"chart-bar-inner\"> <div class=\"chart-bar ca\" style=\"height:${caH}%;opacity:${isCurrent ? 1 : 0.6}\"></div> <div class=\"chart-bar profit\" style=\"height:${netH}%;opacity:${isCurrent ? 1 : 0.6}\"></div> </div> <div class=\"chart-bar-label\" style=\"${isCurrent ? 'color:var(--gold);font-weight:700' : ''}\">${labels[i]}</div> </div>`; }).join(''); attachChartTooltips(container); } function renderChartTrim(year) { const currentM = new Date().getMonth() + 1; const currentT = Math.ceil(currentM / 3); const trims = [ { label: 'T1', months: ['01','02','03'] }, { label: 'T2', months: ['04','05','06'] }, { label: 'T3', months: ['07','08','09'] }, { label: 'T4', months: ['10','11','12'] }, ]; const data = trims.map(t => { const v = db.ventes.filter(x => x.date && x.date.startsWith(year) && t.months.includes(x.date.slice(5,7))); const d = db.depenses.filter(x => x.date && x.date.startsWith(year) && t.months.includes(x.date.slice(5,7))); const ca = v.reduce((s, x) => s + (x.prixVente || 0), 0); const brut = v.reduce((s, x) => s + benefBrut(x), 0); const dep = d.reduce((s, x) => s + (x.montant || 0), 0); const net = brut - calcUrssaf(ca) - dep; return { ca, net: Math.max(0, net) }; }); const maxVal = Math.max(...data.map(d => Math.max(d.ca, d.net)), 1); const container = document.getElementById('chartBars'); container.innerHTML = trims.map((t, i) => { const d = data[i]; const caH = Math.round((d.ca / maxVal) * 100); const netH = Math.round((d.net / maxVal) * 100); const isCurrent = (i + 1) === currentT; return ` <div class=\"chart-bar-group\" data-label=\"${t.label}\" data-ca=\"${d.ca}\" data-net=\"${d.net}\"> <div class=\"chart-bar-inner\"> <div class=\"chart-bar ca\" style=\"height:${caH}%;opacity:${isCurrent ? 1 : 0.6};width:36%\"></div> <div class=\"chart-bar profit\" style=\"height:${netH}%;opacity:${isCurrent ? 1 : 0.6};width:36%\"></div> </div> <div class=\"chart-bar-label\" style=\"${isCurrent ? 'color:var(--gold);font-weight:700' : ''}\">${t.label}</div> </div>`; }).join(''); attachChartTooltips(container); } function attachChartTooltips(container) { container.querySelectorAll('.chart-bar-group').forEach(el => { el.addEventListener('mouseenter', e => { showChartTooltip(e, el.dataset.label, parseFloat(el.dataset.ca), parseFloat(el.dataset.net)); }); el.addEventListener('mousemove', moveChartTooltip); el.addEventListener('mouseleave', hideChartTooltip); }); } // ============================================================ // VENTES // ============================================================ function setupVentePreview() { ['v_achat','v_vente','v_portPaye','v_portRecu'].forEach(id => { document.getElementById(id).addEventListener('input', updateVentePreview); }); } function updateVentePreview() { const achat = parseFloat(document.getElementById('v_achat').value) || 0; const vente = parseFloat(document.getElementById('v_vente').value) || 0; const portPaye = parseFloat(document.getElementById('v_portPaye').value) || 0; const portRecu = parseFloat(document.getElementById('v_portRecu').value) || 0; const preview = document.getElementById('ventePreview'); if (vente === 0) { preview.style.display = 'none'; return; } const brut = vente - achat - portPaye + portRecu; const urssaf = calcUrssaf(vente); const net = brut - urssaf; preview.style.display = 'block'; const elBrut = document.getElementById('prev_brut'); const elNet = document.getElementById('prev_net'); elBrut.textContent = fmt(brut); elBrut.style.color = brut >= 0 ? 'var(--green)' : 'var(--red)'; document.getElementById('prev_urssaf').textContent = fmt(urssaf); elNet.textContent = fmt(net); elNet.style.color = net >= 0 ? 'var(--green)' : 'var(--red)'; } // ============================================================ // SOUS-ONGLETS VENTES // ============================================================ let venteSubTab = 'encours'; function showVenteSubTab(tab, btn) { venteSubTab = tab; document.querySelectorAll('#tab-ventes .sub-tab').forEach(b => b.classList.remove('active')); if (btn) btn.classList.add('active'); document.querySelectorAll('.cmd-panel').forEach(p => p.classList.remove('active')); document.getElementById('vpanel-' + tab).classList.add('active'); if (tab === 'encours') { initECMarqueSelect(); renderVentesEnCours(); } if (tab === 'historique') { initMarqueSelect(); renderVentes(); } } // ============================================================ // VENTES EN COURS // ============================================================ async function addVenteEnCours() { const date = document.getElementById('ec_date').value; const article = getECArticleName(); const prixAchat = parseFloat(document.getElementById('ec_achat').value) || 0; const prixDemande = parseFloat(document.getElementById('ec_demande').value) || 0; const plateforme = document.getElementById('ec_plateforme').value; const notes = document.getElementById('ec_notes').value.trim(); if (!date) { alert('Veuillez saisir une date.'); return; } if (!article) { alert('Veuillez sélectionner une marque et un modèle.'); return; } const { data, error } = await sb.from('ventes_en_cours').insert({ user_id: _uid, date, article, prix_achat: prixAchat, prix_demande: prixDemande, plateforme, notes }).select().single(); if (error) { dbErr(error, 'addVenteEnCours'); return; } if (!db.ventesEnCours) db.ventesEnCours = []; db.ventesEnCours.unshift(mapVenteEnCours(data)); document.getElementById('ec_marque').value = ''; document.getElementById('ec_modele').innerHTML = '<option value=\"\">— Sélectionner un modèle —</option>'; document.getElementById('ec_article').value = ''; document.getElementById('ec_article_group').style.display = 'none'; document.getElementById('ec_achat').value = ''; document.getElementById('ec_demande').value = ''; document.getElementById('ec_notes').value = ''; document.getElementById('ec_date').value = today(); renderVentesEnCours(); } async function deleteVenteEnCours(id) { if (!confirm('Retirer cet article de la liste ?')) return; const { error } = await sb.from('ventes_en_cours').delete().eq('id', id).eq('user_id', _uid); if (error) { dbErr(error, 'deleteVenteEnCours'); return; } db.ventesEnCours = (db.ventesEnCours || []).filter(v => v.id !== id); renderVentesEnCours(); } function renderVentesEnCours() { if (!db.ventesEnCours) db.ventesEnCours = []; const list = [...db.ventesEnCours].sort((a, b) => b.date.localeCompare(a.date)); const tbl = document.getElementById('enCoursTable'); if (!tbl) return; if (list.length === 0) { tbl.innerHTML = `<div class=\"empty-state\"><div class=\"icon\">🏷️</div><p>Aucun article en cours de vente</p><p style=\"font-size:12px;margin-top:6px\">Ajoutez un article ci-dessus pour le mettre en vente</p></div>`; } else { tbl.innerHTML = `<table> <thead><tr> <th>Mise en vente</th><th>Article</th><th>Plateforme</th> <th>Prix d'achat</th><th>Prix demandé</th><th>Notes</th><th></th> </tr></thead> <tbody> ${list.map(v => `<tr> <td>${v.date}</td> <td><strong>${v.article}</strong></td> <td><span class=\"badge badge-gold\">${v.plateforme}</span></td> <td style=\"color:var(--text-muted)\">${v.prixAchat ? fmt(v.prixAchat) : '—'}</td> <td style=\"color:var(--accent);font-weight:700\">${v.prixDemande ? fmt(v.prixDemande) : '—'}</td> <td style=\"font-size:12px;color:var(--text-muted)\">${v.notes || '—'}</td> <td style=\"display:flex;gap:6px;align-items:center;\"> <button class=\"btn-receive\" onclick=\"openVenduModal(${v.id})\">💰 Vendu</button> <button class=\"btn-danger\" onclick=\"deleteVenteEnCours(${v.id})\" title=\"Retirer\">×</button> </td> </tr>`).join('')} </tbody> </table>`; } const tot = document.getElementById('enCoursTotaux'); if (tot) { const total = list.reduce((s, v) => s + (v.prixDemande || 0), 0); tot.innerHTML = ` <div><span style=\"color:var(--text-muted);font-size:12px\">Articles en vente : </span><strong style=\"color:var(--gold)\">${list.length}</strong></div> <div><span style=\"color:var(--text-muted);font-size:12px\">CA potentiel : </span><strong style=\"color:var(--accent)\">${fmt(total)}</strong></div> `; } } // ── MODALE VENDU function openVenduModal(id) { const v = (db.ventesEnCours || []).find(x => x.id === id); if (!v) return; document.getElementById('vendu_ec_id').value = v.id; document.getElementById('vendu_date').value = today(); document.getElementById('vendu_prix').value = v.prixDemande || ''; document.getElementById('vendu_portPaye').value = ''; document.getElementById('vendu_portRecu').value = ''; document.getElementById('vendu_plateforme').value = v.plateforme || 'Vinted'; document.getElementById('vendu_notes').value = v.notes || ''; updateVenduPreview(); document.getElementById('venduModal').classList.add('open'); } function closeVenduModal() { document.getElementById('venduModal').classList.remove('open'); } function updateVenduPreview() { const id = parseInt(document.getElementById('vendu_ec_id').value); const v = (db.ventesEnCours || []).find(x => x.id === id); const prixAchat = v ? (v.prixAchat || 0) : 0; const vente = parseFloat(document.getElementById('vendu_prix').value) || 0; const portPaye = parseFloat(document.getElementById('vendu_portPaye').value) || 0; const portRecu = parseFloat(document.getElementById('vendu_portRecu').value) || 0; const brut = vente - prixAchat - portPaye + portRecu; const urssaf = calcUrssaf(vente); const net = brut - urssaf; const set = (elId, val, colored) => { const el = document.getElementById(elId); el.textContent = fmt(val); el.style.color = colored ? (val >= 0 ? 'var(--green)' : 'var(--red)') : 'var(--accent)'; }; set('vprev_brut', brut, true); set('vprev_urssaf', urssaf, false); set('vprev_net', net, true); } async function confirmVendu() { const id = parseInt(document.getElementById('vendu_ec_id').value); const v = (db.ventesEnCours || []).find(x => x.id === id); if (!v) return; const date = document.getElementById('vendu_date').value; const prixVente = parseFloat(document.getElementById('vendu_prix').value) || 0; const portPaye = parseFloat(document.getElementById('vendu_portPaye').value) || 0; const portRecu = parseFloat(document.getElementById('vendu_portRecu').value) || 0; const plateforme = document.getElementById('vendu_plateforme').value; const notes = document.getElementById('vendu_notes').value.trim(); if (!date) { alert('Veuillez saisir une date.'); return; } if (prixVente <= 0) { alert('Veuillez saisir un prix de vente valide.'); return; } const [insRes, delRes] = await Promise.all([ sb.from('ventes').insert({ user_id: _uid, date, article: v.article, prix_achat: v.prixAchat||0, prix_vente: prixVente, port_paye: portPaye, port_recu: portRecu, plateforme, notes }).select().single(), sb.from('ventes_en_cours').delete().eq('id', id).eq('user_id', _uid) ]); if (insRes.error) { dbErr(insRes.error, 'confirmVendu-insert'); return; } if (delRes.error) { dbErr(delRes.error, 'confirmVendu-delete'); return; } db.ventes.unshift(mapVente(insRes.data)); db.ventesEnCours = (db.ventesEnCours || []).filter(x => x.id !== id); closeVenduModal(); renderVentesEnCours(); renderDashboard(); } async function addVente() { const date = document.getElementById('v_date').value; const article = getArticleName(); const prixAchat = parseFloat(document.getElementById('v_achat').value) || 0; const prixVente = parseFloat(document.getElementById('v_vente').value) || 0; const portPaye = parseFloat(document.getElementById('v_portPaye').value) || 0; const portRecu = parseFloat(document.getElementById('v_portRecu').value) || 0; const plateforme = document.getElementById('v_plateforme').value; const notes = document.getElementById('v_notes').value.trim(); if (!date) { alert('Veuillez saisir une date.'); return; } if (!article) { alert('Veuillez sélectionner une marque et un modèle.'); return; } if (prixVente <= 0) { alert('Veuillez saisir un prix de vente valide.'); return; } const { data, error } = await sb.from('ventes').insert({ user_id: _uid, date, article, prix_achat: prixAchat, prix_vente: prixVente, port_paye: portPaye, port_recu: portRecu, plateforme, notes }).select().single(); if (error) { dbErr(error, 'addVente'); return; } db.ventes.unshift(mapVente(data)); document.getElementById('v_marque').value = ''; document.getElementById('v_modele').innerHTML = '<option value=\"\">— Sélectionner un modèle —</option>'; document.getElementById('v_article').value = ''; document.getElementById('v_article_group').style.display = 'none'; ['v_notes'].forEach(id => document.getElementById(id).value = ''); ['v_achat','v_vente','v_portPaye','v_portRecu'].forEach(id => document.getElementById(id).value = ''); document.getElementById('v_date').value = today(); document.getElementById('ventePreview').style.display = 'none'; renderVentes(); } async function deleteVente(id) { if (!confirm('Supprimer cette vente ?')) return; const { error } = await sb.from('ventes').delete().eq('id', id).eq('user_id', _uid); if (error) { dbErr(error, 'deleteVente'); return; } db.ventes = db.ventes.filter(v => v.id !== id); renderVentes(); } function renderVentes() { // Mise à jour filtre mois const moisSet = new Set(db.ventes.map(v => getMonth(v.date)).filter(Boolean)); const moisSorted = [...moisSet].sort().reverse(); const filterMois = document.getElementById('filterMois'); if (filterMois) { const cur = filterMois.value; filterMois.innerHTML = '<option value=\"\">Tous les mois</option>' + moisSorted.map(m => `<option value=\"${m}\" ${m === cur ? 'selected' : ''}>${monthLabel(m)}</option>`).join(''); } const moisF = document.getElementById('filterMois')?.value || ''; const platF = document.getElementById('filterPlateforme')?.value || ''; let filtered = [...db.ventes]; if (moisF) filtered = filtered.filter(v => getMonth(v.date) === moisF); if (platF) filtered = filtered.filter(v => v.plateforme === platF); filtered.sort((a, b) => b.date.localeCompare(a.date)); document.getElementById('ventesTable').innerHTML = buildVentesTable(filtered, true); const ca = filtered.reduce((s, v) => s + (v.prixVente || 0), 0); const brut = filtered.reduce((s, v) => s + benefBrut(v), 0); const urssaf = calcUrssaf(ca); const net = brut - urssaf; document.getElementById('ventesTotaux').innerHTML = ` <div><span style=\"color:var(--text-muted);font-size:12px\">CA total : </span><strong style=\"color:var(--gold)\">${fmt(ca)}</strong></div> <div><span style=\"color:var(--text-muted);font-size:12px\">Bénéfice brut : </span><strong>${fmt(brut)}</strong></div> <div><span style=\"color:var(--text-muted);font-size:12px\">URSSAF estimé : </span><strong style=\"color:var(--blue)\">${fmt(urssaf)}</strong></div> <div><span style=\"color:var(--text-muted);font-size:12px\">Bénéfice net : </span><strong style=\"color:${net >= 0 ? 'var(--green)' : 'var(--red)'}\">${fmt(net)}</strong></div> <div><span style=\"color:var(--text-muted);font-size:12px\">Nb ventes : </span><strong>${filtered.length}</strong></div> `; } function buildVentesTable(ventes, withDelete) { if (ventes.length === 0) { return `<div class=\"empty-state\"><div class=\"icon\">🛍️</div><p>Aucune vente trouvée</p></div>`; } return `<table> <thead><tr> <th>Date</th><th>Article</th><th>Plateforme</th> <th>Achat</th><th>Vente</th><th>Port payé</th><th>Port reçu</th> <th>Bén. brut</th><th>URSSAF</th><th>Bén. net</th> ${withDelete ? '<th style=\"width:80px\"></th>' : ''} </tr></thead> <tbody> ${ventes.map(v => { const brut = benefBrut(v); const urssaf = calcUrssaf(v.prixVente || 0); const net = benefNet(v); return `<tr> <td>${v.date}</td> <td> <strong>${v.article}</strong> ${v.notes ? `<br><span style=\"font-size:11px;color:var(--text-muted)\">${v.notes}</span>` : ''} </td> <td><span class=\"badge badge-gold\">${v.plateforme}</span></td> <td>${fmt(v.prixAchat)}</td> <td style=\"color:var(--accent);font-weight:700\">${fmt(v.prixVente)}</td> <td>${v.portPaye ? fmt(v.portPaye) : '—'}</td> <td>${v.portRecu ? fmt(v.portRecu) : '—'}</td> <td style=\"color:${brut >= 0 ? 'var(--green)' : 'var(--red)'};font-weight:600\">${fmt(brut)}</td> <td style=\"color:var(--accent)\">${fmt(urssaf)}</td> <td style=\"color:${net >= 0 ? 'var(--green)' : 'var(--red)'};font-weight:700\">${fmt(net)}</td> ${withDelete ? `<td style=\"white-space:nowrap\"> <button class=\"btn-edit\" onclick=\"openEditVente(${v.id})\" title=\"Modifier\">✏️</button> <button class=\"btn-danger\" onclick=\"deleteVente(${v.id})\" title=\"Supprimer\">×</button> </td>` : ''} </tr>`; }).join('')} </tbody> </table>`; } // ============================================================ // DEPENSES // ============================================================ async function addDepense() { const date = document.getElementById('d_date').value; const categorie = document.getElementById('d_categorie').value; const desc = document.getElementById('d_desc').value.trim(); const montant = parseFloat(document.getElementById('d_montant').value) || 0; if (!date) { alert('Veuillez saisir une date.'); return; } if (!desc) { alert('Veuillez saisir une description.'); return; } if (montant <= 0) { alert('Veuillez saisir un montant valide.'); return; } const { data, error } = await sb.from('depenses').insert({ user_id: _uid, date, categorie, description: desc, montant }).select().single(); if (error) { dbErr(error, 'addDepense'); return; } db.depenses.unshift(mapDepense(data)); document.getElementById('d_date').value = today(); document.getElementById('d_desc').value = ''; document.getElementById('d_montant').value = ''; renderDepenses(); } async function deleteDepense(id) { if (!confirm('Supprimer cette dépense ?')) return; const { error } = await sb.from('depenses').delete().eq('id', id).eq('user_id', _uid); if (error) { dbErr(error, 'deleteDepense'); return; } db.depenses = db.depenses.filter(d => d.id !== id); renderDepenses(); } function renderDepenses() { const moisSet = new Set(db.depenses.map(d => getMonth(d.date)).filter(Boolean)); const moisSorted = [...moisSet].sort().reverse(); const filterEl = document.getElementById('filterDepenseMois'); if (filterEl) { const cur = filterEl.value; filterEl.innerHTML = '<option value=\"\">Tous les mois</option>' + moisSorted.map(m => `<option value=\"${m}\" ${m === cur ? 'selected' : ''}>${monthLabel(m)}</option>`).join(''); } const moisF = document.getElementById('filterDepenseMois')?.value || ''; const catF = document.getElementById('filterDepenseCat')?.value || ''; let filtered = [...db.depenses]; if (moisF) filtered = filtered.filter(d => getMonth(d.date) === moisF); if (catF) filtered = filtered.filter(d => d.categorie === catF); filtered.sort((a, b) => b.date.localeCompare(a.date)); if (filtered.length === 0) { document.getElementById('depensesTable').innerHTML = `<div class=\"empty-state\"><div class=\"icon\">💸</div><p>Aucune dépense trouvée</p></div>`; } else { document.getElementById('depensesTable').innerHTML = `<table> <thead><tr><th>Date</th><th>Catégorie</th><th>Description</th><th>Montant</th><th></th></tr></thead> <tbody> ${filtered.map(d => `<tr> <td>${d.date}</td> <td><span class=\"badge badge-gold\">${d.categorie}</span></td> <td>${d.desc}</td> <td style=\"color:var(--red);font-weight:600\">${fmt(d.montant)}</td> <td><button class=\"btn-danger\" onclick=\"deleteDepense(${d.id})\" title=\"Supprimer\">×</button></td> </tr>`).join('')} </tbody> </table>`; } const total = filtered.reduce((s, d) => s + (d.montant || 0), 0); document.getElementById('depensesTotaux').innerHTML = ` <div><span style=\"color:var(--text-muted);font-size:12px\">Total dépenses : </span><strong style=\"color:var(--red)\">${fmt(total)}</strong></div> <div><span style=\"color:var(--text-muted);font-size:12px\">Nombre d'entrées : </span><strong>${filtered.length}</strong></div> `; } // ============================================================ // URSSAF // ============================================================ function renderUrssaf() { const year = currentYear(); document.getElementById('urssafYear').textContent = year; document.getElementById('urssafRateDisplay').textContent = db.parametres.urssaf.toFixed(1).replace('.', ',') + ' %'; const vAnnee = db.ventes.filter(v => v.date && v.date.startsWith(year)); const caAnnee = vAnnee.reduce((s, v) => s + (v.prixVente || 0), 0); const urssafAnnee = calcUrssaf(caAnnee); const seuilPct = Math.min((caAnnee / db.parametres.seuil) * 100, 100).toFixed(1); document.getElementById('urssafCards').innerHTML = ` <div class=\"card gold-border\"> <div class=\"card-label\">CA ${year}</div> <div class=\"card-value\">${fmt(caAnnee)}</div> <div class=\"card-sub\">${seuilPct}% du seuil autorisé</div> <div class=\"progress-bar-wrapper\"><div class=\"progress-bar\" style=\"width:${seuilPct}%\"></div></div> </div> <div class=\"card\"> <div class=\"card-label\">Cotisations totales ${year}</div> <div class=\"card-value blue\">${fmt(urssafAnnee)}</div> <div class=\"card-sub\">Taux : ${db.parametres.urssaf}% du CA</div> </div> <div class=\"card\"> <div class=\"card-label\">Reste avant seuil</div> <div class=\"card-value\">${fmt(Math.max(0, db.parametres.seuil - caAnnee))}</div> <div class=\"card-sub\">Seuil : ${fmt(db.parametres.seuil)}</div> </div> `; const currentM = new Date().getMonth() + 1; const currentT = Math.ceil(currentM / 3); const trimestres = [ { name: 'T1', months: ['01','02','03'], deadline: `30 avril ${year}`, label: 'Jan · Fév · Mar' }, { name: 'T2', months: ['04','05','06'], deadline: `31 juillet ${year}`, label: 'Avr · Mai · Jun' }, { name: 'T3', months: ['07','08','09'], deadline: `31 octobre ${year}`, label: 'Jul · Aoû · Sep' }, { name: 'T4', months: ['10','11','12'], deadline: `31 janvier ${year + 1}`, label: 'Oct · Nov · Déc' }, ]; document.getElementById('urssafTrimestreGrid').innerHTML = trimestres.map((t, i) => { const vT = db.ventes.filter(v => { if (!v.date || !v.date.startsWith(year)) return false; return t.months.includes(v.date.slice(5, 7)); }); const caT = vT.reduce((s, v) => s + (v.prixVente || 0), 0); const cotisT = calcUrssaf(caT); const isCurrent = (i + 1) === currentT; return `<div class=\"urssaf-trimestre ${isCurrent ? 'current' : ''}\"> <div style=\"display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:14px;\"> <div> <div style=\"font-size:16px;font-weight:700;color:var(--text)\">${t.name} — ${year} ${isCurrent ? '<span class=\"badge badge-gold\" style=\"font-size:10px;margin-left:8px;vertical-align:middle\">EN COURS</span>' : ''} </div> <div style=\"font-size:12px;color:var(--text-muted);margin-top:2px\">${t.label}</div> </div> <div style=\"font-size:12px;color:var(--text-muted);text-align:right\">⏰ Échéance<br><strong style=\"color:var(--text)\">${t.deadline}</strong></div> </div> <div style=\"display:flex;justify-content:space-between;\"> <div> <div style=\"font-size:11px;color:var(--text-muted)\">CA du trimestre</div> <div style=\"font-size:22px;font-weight:700;color:var(--gold)\">${fmt(caT)}</div> </div> <div style=\"text-align:right\"> <div style=\"font-size:11px;color:var(--text-muted)\">Cotisations à déclarer</div> <div style=\"font-size:22px;font-weight:700;color:var(--blue)\">${fmt(cotisT)}</div> </div> </div> <div style=\"font-size:12px;color:var(--text-muted);margin-top:10px;border-top:1px solid var(--dark-border);padding-top:8px\">${vT.length} vente(s) ce trimestre</div> </div>`; }).join(''); } // ============================================================ // OBJECTIFS // ============================================================ function renderObjectifs() { const year = currentYear(); document.getElementById('objectifYear').textContent = year; document.getElementById('obj_ca_annuel').value = db.objectifs.ca_annuel || ''; document.getElementById('obj_ca_mensuel').value = db.objectifs.ca_mensuel || ''; document.getElementById('obj_benef_mensuel').value = db.objectifs.benef_mensuel || ''; document.getElementById('obj_nb_ventes').value = db.objectifs.nb_ventes || ''; const now = currentMonth(); const vMois = db.ventes.filter(v => getMonth(v.date) === now); const caMois = vMois.reduce((s, v) => s + (v.prixVente || 0), 0); const brutM = vMois.reduce((s, v) => s + benefBrut(v), 0); const depM = db.depenses.filter(d => getMonth(d.date) === now) .reduce((s, d) => s + (d.montant || 0), 0); const netM = brutM - calcUrssaf(caMois) - depM; const vAnnee = db.ventes.filter(v => v.date && v.date.startsWith(year)); const caAnnee = vAnnee.reduce((s, v) => s + (v.prixVente || 0), 0); const o = db.objectifs; document.getElementById('objectifsProgress').innerHTML = buildProgressItems([ { label: 'CA mensuel', current: caMois, target: o.ca_mensuel, unit: '€' }, { label: 'Bénéfice net mensuel', current: netM, target: o.benef_mensuel, unit: '€' }, { label: 'Nombre de ventes / mois', current: vMois.length, target: o.nb_ventes, unit: 'ventes' }, ]); document.getElementById('objectifsProgressAnnuel').innerHTML = buildProgressItems([ { label: `CA annuel ${year}`, current: caAnnee, target: o.ca_annuel, unit: '€' }, { label: `Ventes totales ${year}`, current: vAnnee.length, target: o.nb_ventes ? o.nb_ventes * 12 : 0, unit: 'ventes' }, ]); } function buildProgressItems(items) { return items.map(item => { const noTarget = !item.target || item.target <= 0; const val = item.unit === '€' ? fmt(item.current) : `${item.current} ${item.unit}`; if (noTarget) return ` <div style=\"background:var(--dark-card);border:1px solid var(--dark-border);border-radius:10px;padding:16px;margin-bottom:12px;\"> <div style=\"font-size:13px;color:var(--text-muted);margin-bottom:6px\">${item.label}</div> <div style=\"font-size:20px;font-weight:700;color:var(--gold)\">${val}</div> <div style=\"font-size:11px;color:var(--text-muted);margin-top:4px\">Aucun objectif défini — définissez-en un ci-dessus</div> </div>`; const pct = Math.min(Math.round((item.current / item.target) * 100), 100); const color = pct >= 100 ? 'var(--green)' : pct >= 75 ? 'var(--gold)' : pct >= 50 ? 'var(--blue)' : 'var(--text)'; const targetVal = item.unit === '€' ? fmt(item.target) : `${item.target} ${item.unit}`; const reste = item.target - item.current; const resteVal = item.unit === '€' ? fmt(Math.abs(reste)) : `${Math.abs(reste)} ${item.unit}`; return ` <div style=\"background:var(--dark-card);border:1px solid var(--dark-border);border-radius:10px;padding:16px;margin-bottom:12px;\"> <div style=\"display:flex;justify-content:space-between;align-items:center;margin-bottom:8px;\"> <div style=\"font-size:13px;color:var(--text-muted)\">${item.label}</div> <div style=\"font-size:13px;font-weight:700;color:${color}\">${pct}%</div> </div> <div style=\"display:flex;justify-content:space-between;align-items:baseline;margin-bottom:8px;\"> <span style=\"font-size:20px;font-weight:700;color:${color}\">${val}</span> <span style=\"font-size:12px;color:var(--text-muted)\">sur ${targetVal}</span> </div> <div class=\"progress-bar-wrapper\"> <div class=\"progress-bar\" style=\"width:${pct}%;background:${pct >= 100 ? 'var(--green)' : 'linear-gradient(90deg,var(--gold),var(--gold-light))'}\"></div> </div> <div style=\"font-size:11px;margin-top:6px;color:${pct >= 100 ? 'var(--green)' : 'var(--text-muted)'}\"> ${pct >= 100 ? '✅ Objectif atteint !' : `Il reste ${resteVal} à atteindre`} </div> </div>`; }).join(''); } async function saveObjectifs() { db.objectifs = { ca_annuel: parseFloat(document.getElementById('obj_ca_annuel').value) || 0, ca_mensuel: parseFloat(document.getElementById('obj_ca_mensuel').value) || 0, benef_mensuel: parseFloat(document.getElementById('obj_benef_mensuel').value) || 0, nb_ventes: parseInt(document.getElementById('obj_nb_ventes').value) || 0, }; const { error } = await sb.from('parametres').upsert({ user_id: _uid, ...db.objectifs, urssaf: db.parametres.urssaf, seuil: db.parametres.seuil, updated_at: new Date().toISOString() }); if (error) { dbErr(error, 'saveObjectifs'); return; } renderObjectifs(); alert('✅ Objectifs sauvegardés !'); } // ============================================================ // PARAMÈTRES // ============================================================ function renderParametres() { document.getElementById('s_urssaf').value = db.parametres.urssaf; document.getElementById('s_seuil').value = db.parametres.seuil; document.getElementById('statsVentes').textContent = db.ventes.length; document.getElementById('statsDepenses').textContent = db.depenses.length; } async function saveParametres() { db.parametres.urssaf = parseFloat(document.getElementById('s_urssaf').value) || 12.3; db.parametres.seuil = parseFloat(document.getElementById('s_seuil').value) || 188700; const { error } = await sb.from('parametres').upsert({ user_id: _uid, ...db.objectifs, urssaf: db.parametres.urssaf, seuil: db.parametres.seuil, updated_at: new Date().toISOString() }); if (error) { dbErr(error, 'saveParametres'); return; } alert('✅ Paramètres sauvegardés !'); } // ============================================================ // EXPORT / IMPORT // ============================================================ function exportData() { const blob = new Blob([JSON.stringify(db, null, 2)], { type: 'application/json' }); const url = URL.createObjectURL(blob); const a = document.createElement('a'); a.href = url; a.download = `anis-luxury-paris_sauvegarde_${today()}.json`; a.click(); URL.revokeObjectURL(url); } function exportCSV() { const rows = [['Date','Article','Plateforme','Prix achat','Prix vente','Port payé','Port reçu','Bén. brut','URSSAF','Bén. net','Notes']]; db.ventes.forEach(v => { const brut = benefBrut(v); const urssaf = calcUrssaf(v.prixVente || 0); const net = benefNet(v); rows.push([ v.date, v.article, v.plateforme, (v.prixAchat||0).toFixed(2), (v.prixVente||0).toFixed(2), (v.portPaye||0).toFixed(2), (v.portRecu||0).toFixed(2), brut.toFixed(2), urssaf.toFixed(2), net.toFixed(2), v.notes || '' ]); }); const csv = rows.map(r => r.join(';')).join('\\n'); const blob = new Blob(['\\uFEFF' + csv], { type: 'text/csv;charset=utf-8;' }); const url = URL.createObjectURL(blob); const a = document.createElement('a'); a.href = url; a.download = `anis-luxury-paris_ventes_${today()}.csv`; a.click(); URL.revokeObjectURL(url); } function importData(event) { const file = event.target.files[0]; if (!file) return; const reader = new FileReader(); reader.onload = async (e) => { try { const imp = JSON.parse(e.target.result); if (!imp.ventes || !imp.depenses) throw new Error(); if (!confirm('Importer ces données ? Les données actuelles seront remplacées.')) return; // Vider la base await Promise.all([ sb.from('ventes').delete().eq('user_id', _uid), sb.from('ventes_en_cours').delete().eq('user_id', _uid), sb.from('depenses').delete().eq('user_id', _uid), sb.from('commandes').delete().eq('user_id', _uid), ]); // Insérer les nouvelles données const toV = (imp.ventes || []).map(v => ({ user_id: _uid, date: v.date, article: v.article, prix_achat: v.prixAchat||0, prix_vente: v.prixVente||0, port_paye: v.portPaye||0, port_recu: v.portRecu||0, plateforme: v.plateforme||'Autre', notes: v.notes||'' })); const toVec = (imp.ventesEnCours || []).map(v => ({ user_id: _uid, date: v.date, article: v.article, prix_achat: v.prixAchat||0, prix_demande: v.prixDemande||0, plateforme: v.plateforme||'Vinted', notes: v.notes||'' })); const toDep = (imp.depenses || []).map(d => ({ user_id: _uid, date: d.date, categorie: d.categorie, description: d.desc||'', montant: d.montant||0 })); const toCmd = (imp.commandes || []).map(c => ({ user_id: _uid, date: c.date, article: c.article, fournisseur: c.fournisseur||'', prix_achat: c.prixAchat||0, statut: c.statut||'En cours', notes: c.notes||'', date_reception: c.dateReception||null })); await Promise.all([ toV.length ? sb.from('ventes').insert(toV) : Promise.resolve(), toVec.length ? sb.from('ventes_en_cours').insert(toVec) : Promise.resolve(), toDep.length ? sb.from('depenses').insert(toDep) : Promise.resolve(), toCmd.length ? sb.from('commandes').insert(toCmd) : Promise.resolve(), ]); await fetchAllData(); renderDashboard(); alert('✅ Données importées avec succès !'); } catch (err) { console.error(err); alert('❌ Fichier invalide. Assurez-vous d\\'utiliser un fichier exporté depuis cette application.'); } }; reader.readAsText(file); event.target.value = ''; } async function clearAllData() { if (!confirm('⚠️ Supprimer TOUTES les données ? Cette action est irréversible.')) return; if (!confirm('Dernière confirmation : toutes les ventes et dépenses seront effacées.')) return; await Promise.all([ sb.from('ventes').delete().eq('user_id', _uid), sb.from('ventes_en_cours').delete().eq('user_id', _uid), sb.from('depenses').delete().eq('user_id', _uid), sb.from('commandes').delete().eq('user_id', _uid), sb.from('parametres').delete().eq('user_id', _uid), ]); db = { ventes: [], ventesEnCours: [], depenses: [], commandes: [], objectifs: { ca_annuel: 0, ca_mensuel: 0, benef_mensuel: 0, nb_ventes: 0 }, parametres: { urssaf: 12.3, seuil: 188700 } }; renderDashboard(); alert('Données effacées.'); } // ============================================================ // INITIALISATION // ============================================================ function init() { document.getElementById('headerDate').textContent = new Date().toLocaleDateString('fr-FR', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' }); document.getElementById('v_date').value = today(); document.getElementById('d_date').value = today(); document.getElementById('c_date').value = today(); initSidebar(); setupVentePreview(); renderDashboard(); } // ============================================================ // SIDEBAR COLLAPSE // ============================================================ function toggleSidebar() { const nav = document.getElementById('sidebar'); const btn = document.getElementById('navToggleBtn'); const collapsed = nav.classList.toggle('collapsed'); localStorage.setItem('alp_nav_collapsed', collapsed ? '1' : '0'); btn.textContent = collapsed ? '❯' : '❮'; } function initSidebar() { if (localStorage.getItem('alp_nav_collapsed') === '1') { const nav = document.getElementById('sidebar'); const btn = document.getElementById('navToggleBtn'); if (nav) nav.classList.add('collapsed'); if (btn) btn.textContent = '❯'; } } // ============================================================ // AUTHENTIFICATION (Supabase) // ============================================================ async function initAuth() {
  // Détecter le retour depuis un lien de réinitialisation
  sb.auth.onAuthStateChange((event, session) => {
    if (event === 'PASSWORD_RECOVERY') {
      showResetPassword();
    }
  });
  const { data: { session } } = await sb.auth.getSession();
  if (session) { _uid = session.user.id; await showApp(); } else { showAuthLogin(); }
} async function showApp() { document.getElementById('authOverlay').style.display = 'none'; document.getElementById('app-content').style.display = ''; await fetchAllData(); init(); initMarqueSelect(); initCMarqueSelect(); } function showAuthLogin() { document.getElementById('authOverlay').style.display = 'flex'; document.getElementById('auth-login').style.display = 'block'; document.getElementById('auth-setup').style.display = 'none'; document.getElementById('auth-forgot').style.display = 'none'; document.getElementById('auth-reset').style.display = 'none'; document.getElementById('app-content').style.display = 'none'; } function showAuthSetup() { document.getElementById('authOverlay').style.display = 'flex'; document.getElementById('auth-setup').style.display = 'block'; document.getElementById('auth-login').style.display = 'none'; document.getElementById('auth-forgot').style.display = 'none'; document.getElementById('auth-reset').style.display = 'none'; document.getElementById('app-content').style.display = 'none'; }

function showForgotPassword() {
  document.getElementById('authOverlay').style.display = 'flex';
  document.getElementById('auth-setup').style.display = 'none';
  document.getElementById('auth-login').style.display = 'none';
  document.getElementById('auth-forgot').style.display = 'block';
  document.getElementById('auth-reset').style.display = 'none';
  document.getElementById('app-content').style.display = 'none';
  const err = document.getElementById('forgot-error');
  const suc = document.getElementById('forgot-success');
  if (err) err.style.display = 'none';
  if (suc) suc.style.display = 'none';
}

async function doForgotPassword() {
  const email = document.getElementById('forgot-user').value.trim();
  if (!email.includes('@')) { showAuthMsg('forgot-error', 'Adresse email invalide.'); return; }
  const btn = document.querySelector('#auth-forgot .btn-gold');
  if (btn) { btn.disabled = true; btn.textContent = 'Envoi en cours...'; }
  const { error } = await sb.auth.resetPasswordForEmail(email, {
    redirectTo: 'https://anis-luxury-paris.netlify.app'
  });
  if (btn) { btn.disabled = false; btn.textContent = '📧 Envoyer le lien de réinitialisation'; }
  if (error) {
    showAuthMsg('forgot-error', 'Erreur lors de l'envoi. Vérifiez l'adresse email.');
  } else {
    const errEl = document.getElementById('forgot-error');
    if (errEl) errEl.style.display = 'none';
    showAuthMsg('forgot-success', '✅ Email envoyé ! Vérifiez votre boîte mail (et les spams).');
    document.getElementById('forgot-user').value = '';
  }
}

function showResetPassword() {
  document.getElementById('authOverlay').style.display = 'flex';
  document.getElementById('auth-setup').style.display = 'none';
  document.getElementById('auth-login').style.display = 'none';
  document.getElementById('auth-forgot').style.display = 'none';
  document.getElementById('auth-reset').style.display = 'block';
  document.getElementById('app-content').style.display = 'none';
}

async function doResetPassword() {
  const password = document.getElementById('reset-pass').value;
  const confirm = document.getElementById('reset-confirm').value;
  if (password.length < 8) { showAuthMsg('reset-error', 'Le mot de passe doit faire au moins 8 caractères.'); return; }
  if (password !== confirm) { showAuthMsg('reset-error', 'Les mots de passe ne correspondent pas.'); return; }
  const btn = document.querySelector('#auth-reset .btn-gold');
  if (btn) { btn.disabled = true; btn.textContent = 'Mise à jour...'; }
  const { error } = await sb.auth.updateUser({ password });
  if (btn) { btn.disabled = false; btn.textContent = '🔐 Changer le mot de passe'; }
  if (error) {
    showAuthMsg('reset-error', 'Erreur : ' + error.message);
  } else {
    showAuthMsg('reset-success', '✅ Mot de passe changé avec succès !');
    setTimeout(() => { window.location.hash = ''; showAuthLogin(); }, 2000);
  }
} async function doLogin() { const email = document.getElementById('login-user').value.trim(); const password = document.getElementById('login-pass').value; if (!email || !password) { showAuthMsg('login-error', 'Email et mot de passe requis.'); return; } const btn = document.querySelector('#auth-login .btn-gold'); if (btn) { btn.disabled = true; btn.textContent = 'Connexion...'; } const { data, error } = await sb.auth.signInWithPassword({ email, password }); if (btn) { btn.disabled = false; btn.textContent = '→ Se connecter'; } if (error) { showAuthMsg('login-error', 'Email ou mot de passe incorrect.'); document.getElementById('login-pass').value = ''; } else { _uid = data.user.id; await showApp(); } } async function doSetup() { const email = document.getElementById('setup-user').value.trim(); const password = document.getElementById('setup-pass').value; const confirm = document.getElementById('setup-confirm').value; if (!email.includes('@')) { showAuthMsg('setup-error', 'Adresse email invalide.'); return; } if (password.length < 8) { showAuthMsg('setup-error', 'Le mot de passe doit faire au moins 8 caractères.'); return; } if (password !== confirm) { showAuthMsg('setup-error', 'Les mots de passe ne correspondent pas.'); return; } const btn = document.querySelector('#auth-setup .btn-gold'); if (btn) { btn.disabled = true; btn.textContent = 'Création...'; } const { data, error } = await sb.auth.signUp({ email, password }); if (btn) { btn.disabled = false; btn.textContent = '🔐 Créer le compte'; } if (error) { showAuthMsg('setup-error', error.message); } else { _uid = data.user?.id; showAuthMsg('setup-success', 'Compte créé ! Connexion en cours...'); setTimeout(async () => { await showApp(); }, 1200); } } async function doLogout() { await sb.auth.signOut(); _uid = null; db = { ventes: [], ventesEnCours: [], depenses: [], commandes: [], objectifs: { ca_annuel: 0, ca_mensuel: 0, benef_mensuel: 0, nb_ventes: 0 }, parametres: { urssaf: 12.3, seuil: 188700 } }; document.getElementById('login-user').value = ''; document.getElementById('login-pass').value = ''; const el = document.getElementById('login-error'); if (el) el.style.display = 'none'; showAuthLogin(); } function showAuthMsg(id, msg) { const el = document.getElementById(id); el.textContent = msg; el.style.display = 'block'; } document.getElementById('login-pass')?.addEventListener('keydown', e => { if (e.key === 'Enter') doLogin(); }); document.getElementById('setup-confirm')?.addEventListener('keydown', e => { if (e.key === 'Enter') doSetup(); }); initAuth(); // ============================================================ // SOUS-ONGLETS COMMANDES // ============================================================ let commandeSubTab = 'transit'; function showCommandeSubTab(tab, btn) { commandeSubTab = tab; document.querySelectorAll('#tab-commandes .sub-tab').forEach(b => b.classList.remove('active')); if (btn) btn.classList.add('active'); document.querySelectorAll('.cmd-panel').forEach(p => p.classList.remove('active')); document.getElementById('cpanel-' + tab).classList.add('active'); if (tab === 'transit') renderTransit(); if (tab === 'reception') renderReception(); if (tab === 'stock') renderStock(); } // ============================================================ // COMMANDES // ============================================================ async function addCommande() { const date = document.getElementById('c_date').value; const article = getCArticleName(); const fournisseur = document.getElementById('c_fournisseur').value.trim(); const prixAchat = parseFloat(document.getElementById('c_prix').value) || 0; const statut = document.getElementById('c_statut').value; const notes = document.getElementById('c_notes').value.trim(); if (!date) { alert('Veuillez saisir une date.'); return; } if (!article) { alert('Veuillez sélectionner une marque et un modèle.'); return; } const { data, error } = await sb.from('commandes').insert({ user_id: _uid, date, article, fournisseur, prix_achat: prixAchat, statut, notes }).select().single(); if (error) { dbErr(error, 'addCommande'); return; } if (!db.commandes) db.commandes = []; db.commandes.unshift(mapCommande(data)); document.getElementById('c_marque').value = ''; document.getElementById('c_modele').innerHTML = '<option value=\"\">— Sélectionner un modèle —</option>'; document.getElementById('c_article').value = ''; document.getElementById('c_article_group').style.display = 'none'; document.getElementById('c_fournisseur').value = 'WA2S FRS'; document.getElementById('c_prix').value = ''; document.getElementById('c_notes').value = ''; document.getElementById('c_date').value = today(); document.getElementById('c_statut').value = 'En cours'; renderTransit(); renderDashboard(); } async function updateStatutCommande(id, statut) { if (!db.commandes) return; const idx = db.commandes.findIndex(c => c.id === id); if (idx === -1) return; const { error } = await sb.from('commandes').update({ statut }).eq('id', id).eq('user_id', _uid); if (error) { dbErr(error, 'updateStatut'); return; } db.commandes[idx].statut = statut; renderDashboard(); } async function deleteCommande(id) { if (!confirm('Supprimer cette commande ?')) return; const { error } = await sb.from('commandes').delete().eq('id', id).eq('user_id', _uid); if (error) { dbErr(error, 'deleteCommande'); return; } db.commandes = (db.commandes || []).filter(c => c.id !== id); renderTransit(); renderDashboard(); } function renderCommandes() { initCMarqueSelect(); if (commandeSubTab === 'transit') renderTransit(); if (commandeSubTab === 'reception') renderReception(); } function renderTransit() { if (!db.commandes) db.commandes = []; const filtered = [...db.commandes] .filter(c => c.statut !== 'Arrivé') .sort((a, b) => b.date.localeCompare(a.date)); const tbl = document.getElementById('transitTable'); if (!tbl) return; if (filtered.length === 0) { tbl.innerHTML = `<div class=\"empty-state\"><div class=\"icon\">📦</div><p>Aucune commande en transit</p></div>`; } else { tbl.innerHTML = `<table> <thead><tr> <th>Date</th><th>Article</th><th>Fournisseur</th> <th>Prix d'achat</th><th>Statut</th><th>Notes</th><th></th> </tr></thead> <tbody> ${filtered.map(c => `<tr> <td>${c.date}</td> <td><strong>${c.article}</strong></td> <td style=\"color:var(--text-sub)\">${c.fournisseur || '—'}</td> <td style=\"color:var(--accent);font-weight:700\">${c.prixAchat ? fmt(c.prixAchat) : '—'}</td> <td> <select class=\"statut-select\" onchange=\"updateStatutCommande(${c.id}, this.value);renderTransit()\"> <option value=\"En cours\" ${c.statut === 'En cours' ? 'selected' : ''}>⏳ En cours</option> <option value=\"En cours d'envoi\" ${c.statut === \"En cours d'envoi\" ? 'selected' : ''}>🚚 En cours d'envoi</option> </select> </td> <td style=\"font-size:12px;color:var(--text-muted)\">${c.notes || '—'}</td> <td><button class=\"btn-danger\" onclick=\"deleteCommande(${c.id})\" title=\"Supprimer\">×</button></td> </tr>`).join('')} </tbody> </table>`; } const total = filtered.reduce((s, c) => s + (c.prixAchat || 0), 0); const enEnvoi = filtered.filter(c => c.statut === \"En cours d'envoi\").length; const enCours = filtered.filter(c => c.statut === 'En cours').length; const tot = document.getElementById('transitTotaux'); if (tot) tot.innerHTML = ` <div><span style=\"color:var(--text-muted);font-size:12px\">Capital engagé : </span><strong style=\"color:var(--accent)\">${fmt(total)}</strong></div> <div><span style=\"color:var(--text-muted);font-size:12px\">⏳ En cours : </span><strong style=\"color:var(--amber)\">${enCours}</strong></div> <div><span style=\"color:var(--text-muted);font-size:12px\">🚚 En envoi : </span><strong style=\"color:var(--blue)\">${enEnvoi}</strong></div> `; } function renderReception() { if (!db.commandes) db.commandes = []; const filtered = [...db.commandes] .filter(c => c.statut === \"En cours d'envoi\") .sort((a, b) => b.date.localeCompare(a.date)); const list = document.getElementById('receptionList'); if (!list) return; if (filtered.length === 0) { list.innerHTML = `<div class=\"empty-state\"><div class=\"icon\">✅</div><p>Aucun colis en attente de réception</p><p style=\"font-size:12px;margin-top:6px\">Passez une commande en \"En cours d'envoi\" pour la voir ici</p></div>`; return; } list.innerHTML = `<div class=\"reception-list\">${filtered.map(c => ` <div class=\"reception-card\"> <div class=\"reception-card-info\"> <div class=\"reception-card-title\">${c.article}</div> <div class=\"reception-card-sub\"> ${c.fournisseur ? `<span>${c.fournisseur}</span> · ` : ''} <span>Commandé le ${c.date}</span> ${c.prixAchat ? ` · <strong style=\"color:var(--accent)\">${fmt(c.prixAchat)}</strong>` : ''} ${c.notes ? ` · <span>${c.notes}</span>` : ''} </div> </div> <button class=\"btn-receive\" onclick=\"marquerRecu(${c.id})\">✅ Reçu</button> </div> `).join('')}</div>`; } async function marquerRecu(id) { if (!db.commandes) return; const idx = db.commandes.findIndex(c => c.id === id); if (idx === -1) return; const dateReception = today(); const { error } = await sb.from('commandes').update({ statut: 'Arrivé', date_reception: dateReception }).eq('id', id).eq('user_id', _uid); if (error) { dbErr(error, 'marquerRecu'); return; } db.commandes[idx].statut = 'Arrivé'; db.commandes[idx].dateReception = dateReception; renderReception(); renderDashboard(); } async function removeFromStock(id) { if (!confirm('Retirer cet article du stock ?')) return; const { error } = await sb.from('commandes').delete().eq('id', id).eq('user_id', _uid); if (error) { dbErr(error, 'removeFromStock'); return; } db.commandes = (db.commandes || []).filter(c => c.id !== id); renderStock(); renderDashboard(); } function renderStock() { if (!db.commandes) db.commandes = []; // Filtre marque const filterMarque = (document.getElementById('filterStockMarque') || {}).value || ''; // Peupler le filtre marques const marques = [...new Set( db.commandes.filter(c => c.statut === 'Arrivé').map(c => { const parts = c.article.split(' '); return parts.length > 1 ? parts[0] : c.article; }) )].sort(); const sel = document.getElementById('filterStockMarque'); if (sel) { const cur = sel.value; sel.innerHTML = '<option value=\"\">Toutes marques</option>' + marques.map(m => `<option value=\"${m}\" ${m === cur ? 'selected' : ''}>${m}</option>`).join(''); } const filtered = [...db.commandes] .filter(c => { if (c.statut !== 'Arrivé') return false; if (filterMarque && !c.article.startsWith(filterMarque)) return false; return true; }) .sort((a, b) => (b.dateReception || b.date).localeCompare(a.dateReception || a.date)); const tbl = document.getElementById('stockTable'); if (!tbl) return; if (filtered.length === 0) { tbl.innerHTML = `<div class=\"empty-state\"><div class=\"icon\">🗄️</div><p>Aucun article en stock</p><p style=\"font-size:12px;margin-top:6px\">Les commandes marquées <strong>Reçu</strong> apparaissent ici</p></div>`; } else { tbl.innerHTML = `<table> <thead><tr> <th>Reçu le</th><th>Article</th><th>Fournisseur</th> <th>Prix d'achat</th><th>Commandé le</th><th>Notes</th><th></th> </tr></thead> <tbody> ${filtered.map(c => `<tr> <td><strong style=\"color:var(--green)\">${c.dateReception || '—'}</strong></td> <td><strong>${c.article}</strong></td> <td style=\"color:var(--text-sub)\">${c.fournisseur || '—'}</td> <td style=\"color:var(--accent);font-weight:700\">${c.prixAchat ? fmt(c.prixAchat) : '—'}</td> <td style=\"color:var(--text-muted);font-size:12px\">${c.date}</td> <td style=\"font-size:12px;color:var(--text-muted)\">${c.notes || '—'}</td> <td><button class=\"btn-danger\" onclick=\"removeFromStock(${c.id})\" title=\"Retirer du stock\">×</button></td> </tr>`).join('')} </tbody> </table>`; } const total = filtered.reduce((s, c) => s + (c.prixAchat || 0), 0); const tot = document.getElementById('stockTotaux'); if (tot) tot.innerHTML = ` <div><span style=\"color:var(--text-muted);font-size:12px\">Articles en stock : </span><strong style=\"color:var(--gold)\">${filtered.length}</strong></div> <div><span style=\"color:var(--text-muted);font-size:12px\">Capital immobilisé : </span><strong style=\"color:var(--accent)\">${fmt(total)}</strong></div> `; } function statutBadge(s) { if (s === 'Arrivé') return `<span class=\"badge badge-green\">${s}</span>`; if (s === \"En cours d'envoi\") return `<span class=\"badge badge-transit\">🚚 ${s}</span>`; return `<span class=\"badge badge-amber\">⏳ ${s}</span>`; } // ============================================================ // MODIFICATION VENTE — MODALE // ============================================================ function openEditVente(id) { const v = db.ventes.find(x => x.id === id); if (!v) return; document.getElementById('edit_id').value = v.id; document.getElementById('edit_date').value = v.date; document.getElementById('edit_article').value = v.article; document.getElementById('edit_achat').value = v.prixAchat || ''; document.getElementById('edit_vente').value = v.prixVente || ''; document.getElementById('edit_portPaye').value = v.portPaye || ''; document.getElementById('edit_portRecu').value = v.portRecu || ''; document.getElementById('edit_plateforme').value = v.plateforme; document.getElementById('edit_notes').value = v.notes || ''; updateEditPreview(); document.getElementById('editModal').classList.add('open'); } function closeEditModal() { document.getElementById('editModal').classList.remove('open'); } function updateEditPreview() { const achat = parseFloat(document.getElementById('edit_achat').value) || 0; const vente = parseFloat(document.getElementById('edit_vente').value) || 0; const portPaye = parseFloat(document.getElementById('edit_portPaye').value) || 0; const portRecu = parseFloat(document.getElementById('edit_portRecu').value) || 0; const brut = vente - achat - portPaye + portRecu; const urssaf = calcUrssaf(vente); const net = brut - urssaf; const set = (id, val, pos) => { const el = document.getElementById(id); el.textContent = fmt(val); el.style.color = pos ? (val >= 0 ? 'var(--green)' : 'var(--red)') : 'var(--accent)'; }; set('eprev_brut', brut, true); set('eprev_urssaf', urssaf, false); set('eprev_net', net, true); } async function saveEditVente() { const id = parseInt(document.getElementById('edit_id').value); const idx = db.ventes.findIndex(x => x.id === id); if (idx === -1) return; const date = document.getElementById('edit_date').value; const article = document.getElementById('edit_article').value.trim(); const prixVente = parseFloat(document.getElementById('edit_vente').value) || 0; if (!date) { alert('Veuillez saisir une date.'); return; } if (!article) { alert('Veuillez saisir le nom de l\\'article.'); return; } if (prixVente <= 0) { alert('Prix de vente invalide.'); return; } const updated = { date, article, prix_achat: parseFloat(document.getElementById('edit_achat').value) || 0, prix_vente: prixVente, port_paye: parseFloat(document.getElementById('edit_portPaye').value) || 0, port_recu: parseFloat(document.getElementById('edit_portRecu').value) || 0, plateforme: document.getElementById('edit_plateforme').value, notes: document.getElementById('edit_notes').value.trim(), }; const { error } = await sb.from('ventes').update(updated).eq('id', id).eq('user_id', _uid); if (error) { dbErr(error, 'saveEditVente'); return; } db.ventes[idx] = mapVente({ ...db.ventes[idx], ...updated, id }); closeEditModal(); renderVentes(); } // Fermer en cliquant hors de la modale document.getElementById('editModal').addEventListener('click', function(e) { if (e.target === this) closeEditModal(); });"
```